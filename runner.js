"use strict";

/*
function checkAge(age) {
    return age > 18 ? "true" : confirm(`Parents allowed?`);
}

checkAge(21);*/

/*
function askQuestion(question, yes, no) {
    if (confirm(question)){
        yes();
    }else {
        no();
    }
}

/!*let showOK = function (){
    alert("OK");
};

let showCancel = function (){
    alert("CANCEL");
};*!/

askQuestion("Are you agree?", function () {alert("OK")}, function (){alert("Cancel")});*/


/*
let sayHi = (name, age) => console.log(`Hello, ${name} your age is ${age}`);

sayHi("Igor", 32);

let sayBye = () => console.log(`Bye Bye Bye`)

sayBye();

let sum = (a,b) => {
    let result = a + b;
    return alert(result);
}

sum(2,2);

let age = prompt("Please enter your age: ", " ");

let welcome = age >= 18 ?
    () => alert("Please come in!") :
    () => alert("Please go home!");

welcome();*/

/*function ask(question, yes, no) {
    if (confirm(question)) yes()
    else no();
}

ask(
    "Вы согласны?",
    function() { alert("Вы согласились."); },
    function() { alert("Вы отменили выполнение."); }
);*/

/*let ask = (question, yes, no) => {
    if (confirm(question)){
        yes();
    }else {
        no();
    }
}

ask("Do you agree?",
    () => alert("You agreed!"),
    () => alert("You disagree!")
    )*/

