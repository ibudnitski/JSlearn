/*let calc = {
    read() {
      this.a = +prompt("a?", "");
      this.b = +prompt("b?", "");
    },
    sum() {
        return this.a + this.b;
    },
    mul() {
        return this.a * this.b;
    }
}

calc.read();
alert(calc.sum());
alert(calc.mul());*/

/*let user = {
    makeUser() {
        this.name = prompt("Please enter name:", " ");
        this.age = +prompt("Please enter age:", " ");
    },

    showInfo(){
        let info = () => alert(`Arrow function ${this.name} and ${this.age}`);
        info();
    }
}

user.makeUser();
user.showInfo();*/
